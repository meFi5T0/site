<% @post.tags.build %>
<%= form_for(@post) do |post_form| %>
  <% if @post.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= post_form.label :name %><br>
    <%= post_form.text_field :name %><br>
  </div>
  <div class="field">
    <%= post_form.label :title %><br>
    <%= post_form.text_field :title %><br>
  </div>
  <div class="field">
  <%= post_form.label :content %><br>
  <%= post_form.cktext_area :content, :toolbar=>'Full', :width=>'560px', :heigth=>'200px' %>

    <br>
  </div>

  <%= render :partial => 'tags/form', :locals => {:form => post_form} %><br>
  <div class="actions">
    <%= post_form.submit %><br>
  </div>
<% end %>



