# Use the README file as a reference to customize settings.

Post:
  fields:
    default: title
    form: title, content
    options:
    templates:
    content: text_with_ckeditor
  relationships: tags, comments
  application: Application
